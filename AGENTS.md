# このプロジェクトでの作業規則

## 依頼と参照先

- 日常のタスク・プロジェクト・作業時間の操作は [manage-local-tasks](.agents/skills/manage-local-tasks/SKILL.md) に従う。
- ソース・画面・DB・テスト・配布・運用手順の変更は [update-task-manager-system](.agents/skills/update-task-manager-system/SKILL.md) に従う。
- 目的を判別できず実データへの変更に影響する場合だけ、変更前に1点確認する。
- 設計は [Vaultの入口](docs/TaskManager-Vault/00_入口.md) から必要なノートだけ読む。規則はここ、手順はSkill、仕様はVaultを正本とし、同じ説明を複数箇所へ転載しない。

## 変更の規模

- 現在の依頼を満たす最小限の変更を選び、既存の構造・機能・依存関係をまず利用する。
- 将来の仮説だけを理由に、抽象化、共通基盤、設定項目、依存パッケージ、別サービスを追加しない。必要性が具体的な場合だけ導入する。
- 無関係なリファクタリングや一括整形を混ぜない。小さな作業に新しい手順書・補助スクリプト・網羅的なチェックリストを増やさない。
- 確認は、対象の曖昧さや結果に影響する不足情報がある場合に絞る。承認済みの範囲や通常の実装判断を再確認しない。

## 実装とシステム境界

- 対象は `net10.0-windows` のみ。
- 関数先頭には機能を表す日本語コメントを1行、機能ブロック・数値計算には処理概要、メンバー変数には保持内容を短く記す。
- 変数名は意味の分かる単語を使い、一文字名や不要な略語・頭字語だけの名前を避ける。
- OpenAI API、Apps Script、Google Sheets同期、外部公開用サーバーを追加しない。
- Web待受は `127.0.0.1:48120` 限定。AndroidはTailscale ServeのHTTPS中継だけを許可し、Funnelは使わない。
- Serveは本人限定のGrant・端末承認で認証し、本体で正規Host・Origin・Serve利用者情報とPC固有API制限を検証する。独自ログイン・パスワード・OAuth/OIDC認証は追加しない。
- 本体のGoogle Calendar同期権限は `calendar.readonly` 限定。ユーザーが予定変更を明示した場合の接続済みGoogle Calendarプラグインには、この制約を適用しない。

## データと文書

- 正本DBは `%LOCALAPPDATA%\TaskManager\task-manager.db`。移行元XLSXやGoogleスプレッドシートへ書き戻さない。破壊的変更の前にはSQLiteバックアップを作成する。
- DB・バックアップ・資格情報・OAuthトークンをGitへ追加しない。これらの内容や実タスク・顧客・予定・作業ログをVaultへ記録しない。
- 変更後にVaultへの影響を確認し、影響するノートだけ同じ作業で更新する。実装と食い違う場合はソース・マイグレーション・テストで確認する。Vaultには現在仕様だけを簡潔に記し、変更履歴やソースの逐語的転載を追加しない。
